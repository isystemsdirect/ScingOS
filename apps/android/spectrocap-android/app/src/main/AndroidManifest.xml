<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <uses-permission android:name="android.permission.CAMERA" />

    <!-- Required permissions -->
    <uses-permission android:name="android.permission.INTERNET" />

    <application android:usesCleartextTraffic="true"
        android:networkSecurityConfig="@xml/network_security_config"
        android:allowBackup="true"
        android:icon="@drawable/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@drawable/ic_launcher"
        android:supportsRtl="true"
        android:theme="@style/Theme.SpectroCAP">

        <activity
            android:name=".HomeActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Keep MainActivity available (advanced modes), but not launcher -->
        <activity
            android:name=".MainActivity"
            android:exported="false" />

        <!-- Settings Activity -->
        <activity
            android:name=".SettingsActivity"
            android:exported="false"
            android:parentActivityName=".SettingsHubActivity" />

        <!-- Settings Hub (card-based) -->
        <activity
            android:name=".SettingsHubActivity"
            android:exported="false"
            android:parentActivityName=".HomeActivity" />

        <!-- Legal Document Activities -->
        <activity
            android:name=".TermsActivity"
            android:exported="false"
            android:parentActivityName=".SettingsActivity" />

        <activity
            android:name=".PrivacyActivity"
            android:exported="false"
            android:parentActivityName=".SettingsActivity" />

        <activity
            android:name=".OssActivity"
            android:exported="false"
            android:parentActivityName=".SettingsActivity" />

        <!-- Versioning Activity -->
        <activity
            android:name=".VersioningActivity"
            android:exported="false"
            android:parentActivityName=".SettingsActivity" />

    </application>

</manifest>


