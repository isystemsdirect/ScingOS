
'''''''''''''''''''''{
  "name": "scing-firebase-mcp",
  "description": "SCINGULAR MCP bridge for Firebase and Next.js analysis",
  "version": "1.0.0",
  "servers": [
    {
      "id": "Scing-MCP",
      "type": "stdio",
      "command": "node",
      "args": ["mcp/server.mjs"],
      "env": {}
    },
    {
      "id": "ScingGPT-MCP",
      "type": "stdio",
      "command": "node",
      "args": ["G:/GIT/isystemsdirect/scinggpt-mcp/dist/server.js"],
      "env": { "NODE_OPTIONS": "--enable-source-maps" }
    }
  ],
  "tools": [
    {
      "name": "analyze_firebase_status",
      "description": "Analyze Firebase Studio and Next.js app status reports",
      "inputSchema": {
        "type": "object",
        "properties": {
          "repoReport": { "type": "string" },
          "appReport": { "type": "string" }
        },
        "required": ["repoReport", "appReport"]
      }
    }
  ]
}
}
