// Optional: RTSF live adapter stub.
// This is intentionally a no-op until a concrete RTSF live feed exists.

export const startRtsfLiveAdapter = (): (() => void) => {
	return () => {
		// no-op
	};
};
