import RightRail from './RightRail'

/**
 * Legacy compatibility alias.
 * The canonical top-right dev UI container is now `RightRail`.
 */
export default function RightStack() {
  return <RightRail />
}
