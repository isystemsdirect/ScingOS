export const LAYER_ENV = 0
// Execution pack: disable layer masking so the avatar cannot be hidden by camera/light layer configuration.
// Keep the symbol for compatibility with reflection code paths.
export const LAYER_AVATAR = 0
