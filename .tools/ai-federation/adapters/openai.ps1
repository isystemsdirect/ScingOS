param([Parameter(Mandatory=$true)][string]$Prompt,[string]$Model="gpt-4.1", [switch]$WantCitations)
throw "OPENAI adapter stub. Implement when you decide endpoint + key env var (e.g. OPENAI_API_KEY)."
